import yfinance as yf, json, os, sys;from datetime import datetime;def handler(event, context):data=yf.download("GC=F",period="1mo",interval="1h");signal="HOLD";if not data.empty:curr_price=data['Close'].iloc[-1];prev_price=data['Close'].iloc[-2];signal="BUY" if curr_price>prev_price else "SELL";return {"statusCode":200,"body":json.dumps({"signal":signal,"price":float(curr_price),"timestamp":str(datetime.now())})};
