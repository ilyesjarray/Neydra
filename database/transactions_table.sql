CREATE TABLE IF NOT EXISTS transactions (id INT AUTO_INCREMENT PRIMARY KEY, tx_hash VARCHAR(66) NOT NULL UNIQUE, from_address VARCHAR(42) NOT NULL, to_address VARCHAR(42) NOT NULL, amount DECIMAL(36, 18) NOT NULL, block_number INT NOT NULL, processed BOOLEAN DEFAULT FALSE);
