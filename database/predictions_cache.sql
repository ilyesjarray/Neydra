CREATE TABLE IF NOT EXISTS predictions_cache (id INT AUTO_INCREMENT PRIMARY KEY, asset VARCHAR(10) DEFAULT 'XAU/USD', signal ENUM('BUY', 'SELL', 'HOLD') NOT NULL, confidence DECIMAL(5, 2) NOT NULL, price DECIMAL(18, 2) NOT NULL, timestamp TIMESTAMP DEFAULT CURRENT_TIMESTAMP);
